# TODO

https://github.com/DiffSK/configobj

- [ ] Remove spaces around the equal sign in the proxy configuration file. This is a known issue with the current implementation and should be fixed to ensure proper parsing of the configuration file.
- [ ] Force adding quotes around the values in the proxy configuration file. This is a known issue with the current implementation and should be fixed to ensure proper parsing of the configuration file.